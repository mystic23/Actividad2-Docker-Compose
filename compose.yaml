version: '3'
services:
  proxy:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
  app1:
    build: ./App1
    ports:
      - "8000:8000"
  app2:
    build: ./App2
    ports:
      - "8001:8001"
